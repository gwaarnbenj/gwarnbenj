let isApproved = false;

let person = {
    name: 'Benj',
    lastName: 'Rheeston'
};

// Dot notation
person.name = 'Benjamin';

console.log(person.name);

//Arrays
let selectedColours = ['red', 'Green', 'blue']; //square brackets indicate empty array
console.log(selectedColours);

//Functions - Set of statements that performs a task (performing a task) 
function hello(name, lastName) {
    console.log('Hello ' + name + ' ' + lastName);
}

// Function that calculates a value
function square(number) {
    number * number;
}

square(2);



